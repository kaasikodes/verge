import { describe, expect, it } from "vitest";

import { render, screen } from "@testing-library/react";
import GlobalSupport from "../GlobalSupport";
describe("Layout", () => {
  describe("AppSubHeader", () => {
    it.todo("Should display 6 link btns", () => {});
    it.todo("Should display a search bar", () => {});
  });
});

// TODO: Add other todo for other components tests and proceed to implement later
