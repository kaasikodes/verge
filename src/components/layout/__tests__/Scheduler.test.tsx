import { describe, expect, it } from "vitest";

import { render, screen } from "@testing-library/react";
import GlobalSupport from "../GlobalSupport";
describe("Sheduler", () => {
  describe("CalenderBtn", () => {
    it.todo("Should display drawer when clicked", () => {});
  });
  describe("CalenderDrawer", () => {
    it.todo("Should display a drawer with date picker", () => {});
    it.todo("The Drawer should display a close btn", () => {});
    it.todo("The Drawer should close when user clicks on close btn", () => {});
  });
});
