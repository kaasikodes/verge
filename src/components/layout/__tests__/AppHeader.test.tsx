import { describe, expect, it } from "vitest";

import { render, screen } from "@testing-library/react";
import GlobalSupport from "../GlobalSupport";
describe("Layout", () => {
  describe("AppHeader", () => {
    it.todo(
      "Should render a company logo link to points to dashboard",
      () => {}
    );
    it.todo("Should render 3 action icon buttons", () => {});
    it.todo("Should render user name", () => {});
  });
});
